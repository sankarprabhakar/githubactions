name: matrix

on:
  workflow_dispatch:

jobs:
  matrix-jobs:
    strategy:
      matrix:
        nodeversions: [10, 12]
    uses: ./gihub/workflows/printjob.yml
    with:
      node: ${{matrix.nodeversions}}